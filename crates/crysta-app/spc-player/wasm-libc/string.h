/* Served by Rust's compiler builtins on wasm32. */
#ifndef SPC_WASM_STRING_H
#define SPC_WASM_STRING_H
#include <stddef.h>
void *memcpy(void *destination, const void *source, size_t size);
void *memset(void *destination, int value, size_t size);
#endif
