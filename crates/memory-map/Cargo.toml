[package]
name = "memory-map"
description = "Versioned memory-symbol schema and ca65 include generation for Terranigma"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
rom = { path = "../rom" }
